//1
/*const name = 'Tray'
console.log('name', name, typeof name);

const age = 23
console.log('age', age, typeof age);

const player = true
console.log('player', player, typeof player);

const number = Symbol('7')
console.log('number', number, typeof number);

const breaking = null
console.log('breaking', breaking, typeof breaking);

const partner = {
	name: 'Bogard',
	age: 25,
	player: true
}
console.log('partner', partner, typeof partner);

const driver = undefined
console.log('driver', driver, typeof driver);

const moreNum = BigInt(25)
console.log('moreNum', moreNum, typeof moreNum);*/
